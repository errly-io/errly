# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/errly?schema=public"

# NextAuth.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-change-in-production

# Google OAuth (optional - leave empty if not using)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# For development, you can generate a secret with:
# openssl rand -base64 32
AUTH_SECRET=your-secret-key-here-change-in-production

# Go API Server Configuration
# Server
PORT=8080
HOST=0.0.0.0
ENVIRONMENT=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=errly
DB_PASSWORD=your_database_password
DB_NAME=errly
DB_SSL_MODE=disable

# ClickHouse
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=9000
CLICKHOUSE_USER=errly
CLICKHOUSE_PASSWORD=your_clickhouse_password
CLICKHOUSE_DATABASE=errly_events

# Redis
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=your-jwt-secret-change-in-production

# Rate Limits
INGEST_RPM=1000
API_RPM_PER_KEY=100
BURST_SIZE=50
